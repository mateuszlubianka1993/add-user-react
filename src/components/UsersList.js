import React from 'react';

class UsersList extends React.Component {

    render() {
        // const listOfUsers = this.props.users.map((user, index)=>)

        return(
            <div className="">List</div>
        );
    };
};

export default UsersList;
